services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    volumes: 
      - myapp:/home/postgres

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
volumes:
  myapp:
